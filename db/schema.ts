import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const workspaces=sqliteTable('workspaces',{id:text('id').primaryKey(),name:text('name').notNull(),created:text('created').notNull()});
export const members=sqliteTable('members',{userId:text('user_id').primaryKey(),workspaceId:text('workspace_id').notNull(),name:text('name').notNull(),email:text('email').notNull(),role:text('role').notNull() },t=>[index('members_workspace').on(t.workspaceId)]);
export const invites=sqliteTable('invites',{token:text('token').primaryKey(),workspaceId:text('workspace_id').notNull(),role:text('role').notNull(),expires:text('expires').notNull(),usedBy:text('used_by')});
export const journeys=sqliteTable('journeys',{id:text('id').primaryKey(),workspaceId:text('workspace_id').notNull(),ownerId:text('owner_id').notNull(),supervisorId:text('supervisor_id'),shared:integer('shared').notNull().default(0),status:text('status').notNull().default('draft'),version:integer('version').notNull().default(1),data:text('data').notNull(),updated:text('updated').notNull()},t=>[index('journeys_owner').on(t.ownerId),index('journeys_supervisor').on(t.supervisorId),index('journeys_workspace').on(t.workspaceId)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),journeyId:text('journey_id').notNull(),actorId:text('actor_id').notNull(),actorName:text('actor_name').notNull(),kind:text('kind').notNull(),snapshot:text('snapshot').notNull(),created:text('created').notNull()},t=>[index('events_journey').on(t.journeyId)]);
